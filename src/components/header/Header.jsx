export function Header() {
    const headerStyle = {
        fontSize: '30px',
        backgroundColor: 'yellow',
    };

    return (
        <header style={headerStyle}>
            HEADER CONTENT
        </header>
    );
}